---
- name: Deploy Docker Container
  hosts: dev
  become: yes

  tasks:

    - name: Run Docker Container
      docker_container:
        name: webapp
        image: suprit43/dockerpr-webapp:latest
        state: started
        ports:
          - "8085:80"